# 仓库协作约定

- 这是多个 GitHub 开源项目的研究总仓库，文档默认使用简体中文。
- 根 README 以摘要、有序索引和代表性图片为主；长篇分析写入子项目。
- 真实研究项目放在 `projects/NNN-slug/`，编号从 `001` 开始递增。编号保持稳定，不因状态变化或归档重新编号，不复用已有编号。
- 新项目从 `templates/project/` 复制。模板不作为真实项目加入索引。
- 新增或调整项目时，同步维护根 README 的索引和图览，按编号升序排列。
- 子项目必须记录原始仓库、研究版本或提交、研究状态；未验证的结论和链接明确标注，不虚构演示地址或截图。
- 图片放在对应项目的 `assets/`，使用相对路径和有意义的替代文本，并记录外部素材来源。
- 子项目运行依赖、配置和说明保存在自身目录。根据实际项目选择技术栈，根目录不强制统一运行环境。
- Web 演示预留在子项目 `web/`，部署路径见 `docs/DEPLOYMENT.md`。确有演示需要时再增加构建和部署配置。
- 保留引用代码的上游许可；不提交密钥、依赖目录、构建产物或未说明来源的第三方仓库副本。
- 文档变更检查相对链接和目录一致性；代码变更按对应子项目说明执行必要验证。

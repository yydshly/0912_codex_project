# 网页验证记录

日期：2026-09-11。独立研究站点与欧姆定律互动案例；已安装并使用官方 renderer / DSL，没有运行完整上游应用或模型。

## 本次验证结果

- 7 组自动检查通过：电路计算规律与异常输入、项目数值边界、测验与单位换算、受损状态恢复、官方 DSL 场景校验、官方课件组件服务端渲染、学习记录导出。
- 三页课件通过 `@openmaic/dsl 0.11.1` 的 `validateScene`；`@openmaic/renderer 0.1.6` 的 `SlideCanvas` 实际产生了包含三页公式内容的服务端 HTML。这验证组件可运行，不等于浏览器布局与高亮已实测。
- 完成 1 个总览、1 个互动课堂、12 份参考文档的构建（含六模块底层原理、理解汇总与场景选型）。站内链接、锚点、图片替代文本及六个能力入口检查通过。
- 课堂模块和依赖完整打包，检查没有未打包的运行时外部模块；法律注释和已安装生产依赖许可随产物保留。总览 JavaScript 语法检查通过。
- npm 安装完成后的审计结果为 0 个已报告漏洞；这是当时的包数据库结果，不代表未来或所有安全问题。
- 本地 HTTP 案例页面及主要资源已检查；公网尚未发布，不把预期部署地址标记为可用。
- GitHub Pages 流程已增加 Node.js、锁定依赖安装、案例检查与构建；总项目继续保留 004、005 两个子站。

## 验证边界

- 本次未请求浏览器自动化测试，未进行浏览器截图、点击或 DOM 检查；实际滑杆、下载、存储、语音、响应式和辅助技术兼容性尚未通过浏览器实测。
- 已实现键盘焦点、表单标签、状态提示、减少动态效果偏好和窄屏布局规则；这些是实现项，不冒充无障碍认证。
- 角色对话与讲解预先编写；参数实验和评分使用本案例的规则程序，不能代表上游模型生成内容的正确率。
- 未测量完整上游的生成耗时、费用、知识准确性、模型评分一致性或教学收益。

案例能力映射与版本来源见[案例说明](../notes/05-case-study.md)，重现命令见[网页运行说明](README.md)。
